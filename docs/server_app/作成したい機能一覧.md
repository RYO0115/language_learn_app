# server_app 作成したい, 修正したい機能一覧

- [x] 3択テスト
    - [x] 一部の問題で答えを問う箇所が空欄にならない時がある
        - [x] 恐らく過去形など単語の形が変わると空欄にできないように見える
            - 対応済み: `_blank_word_in_sentence` で不規則動詞テーブル・規則変化（過去形・複数形・進行形など）の活用形候補も空欄化の対象にした。テーブルにない不規則動詞・特殊な活用形は依然対応できない場合がある（`docs/server_app/設計書.md` の「既知の制約」参照）。
    - [x] 他の選択肢でも意味が通ってしまうことがある
        - [x] どうすればいいか解決策を考えてください。
            - 対応済み: 誤答選択肢の類似度スコアに意味の重複度ペナルティを追加し、綴りが似ていても意味がほぼ同じ（類義語）候補は順位を下げるようにした（`similarity_service.py` の `_similarity_score`）。ただし汎用的な例文（例: "I saw a ___ yesterday."）では同じ品詞ならどの単語を入れても文として成立してしまう構造的な限界があり、これは意味の重複度だけでは解消できない。より厳密に解決するには、例文ごとに AI が文脈適合性を判定して誤答選択肢を選ぶ仕組み（単語登録時の AI 呼び出し回数増・データモデル変更が必要）が必要になるため、別途要望があれば検討する。

- [x] ドキュメント類
    - [x] docs/server_appに置かれている設計書、仕様書、機能一覧、テスト一覧が最新情報になっているかを確認してください
        - 対応済み: 設計書・仕様書・機能一覧・テスト一覧のテーブル・スキーマ・エンドポイント・テストケースを実装（router/service/models、テンプレート、CI ワークフロー）と全件突き合わせし、不整合がないことを確認した（いずれも最新コードを反映済み）。
    - [x] 機能一覧.mdの## クイズ(quiz)について、単語テストと穴埋めテストの二種類存在し、どの機能がどのテストに使われているのかが分かりづらいです。分かりやすいように修正してください。また他にも似たような書かれ方をしている箇所があれば合わせて修正してください。
        - 対応済み: 機能一覧.md の「クイズ (quiz)」セクションを「両テスト共通」「`meaning`（意味確認テスト）固有」「`fill_blank`（例文穴埋め3択テスト）固有」の3区分に分けて再構成した。他のセクション（単語管理・AI連携・インポート/エクスポート等）は機能ごとに明確に分離されており、同様の分かりづらさは見つからなかった。

- [x] 穴埋め3択テスト
    - [x] 拡張性に乏しいハードコードが多く存在しているように見える
        - [x] 活用形テーブルなどで単語のリストを用意するのは自分で自分の単語帳を作る、というこのアプリのコンセプトに反しているので、まずは問題となる箇所のリストアップを行う
            - 対応済み: 該当箇所は `quiz/service.py` の `_IRREGULAR_VERB_FORMS`（約55個の不規則動詞活用形を直接記述した辞書）の1箇所のみ。他（`WordSimilarity`・`_regular_inflections` 等）はユーザー登録データから動的に生成しており該当しない。詳細は `docs/server_app/過去資料/穴埋め3択テスト_活用形ハードコード調査報告書.md` を参照。
        - [x] 報告書を作成し、改善案を検討してほしい
            - 対応済み: `docs/server_app/過去資料/穴埋め3択テスト_活用形ハードコード調査報告書.md` を作成。改善案として形態素解析ライブラリ（lemminflect）の導入を採用し、`_IRREGULAR_VERB_FORMS` と `_regular_inflections` を `_inflected_forms`（lemminflect 呼び出し + OOV フォールバック）に置き換えて実装完了（対応範囲を名詞・形容詞の不規則変化にも拡張）。AI生成による活用形保存案は呼び出しコスト過剰のため見送り。再発防止のコーディングルールを `CLAUDE.md` に追記した。