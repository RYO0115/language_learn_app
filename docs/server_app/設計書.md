# server_app 設計書

最終更新: 2026-06-19 / バージョン: 0.2.9

## 概要

日本語話者向け英語学習アプリのバックエンドサーバー。
FastAPI + SQLite + Jinja2 によるサーバーサイドレンダリング構成。

---

## アーキテクチャ

### 全体構成

```
server_app/
├── pyproject.toml
└── src/language_learn/
    ├── main.py          # アプリ起動・ルーター登録・DB初期化
    ├── config.py        # 設定管理（Pydantic BaseSettings）
    ├── core/
    │   ├── database.py  # SQLAlchemy エンジン・セッション
    │   ├── base_model.py # Declarative Base・共通カラム
    │   └── exceptions.py # カスタム例外
    └── features/        # 機能ベースモジュール構成
        ├── words/
        ├── quiz/
        ├── streak/
        ├── ai/
        ├── export/
        └── settings/
```

### テスト構成

```
server_app/tests/
├── ui/
│   ├── conftest.py  # live_server / rate_limited_server フィクスチャ（uvicorn をサブプロセス起動）
│   ├── helpers.py    # 単語追加などの共通ヘルパー
│   └── test_*.py     # ページ・フロー別のブラウザ駆動テスト（Playwright + pytest-playwright）
└── unit/
    └── test_*.py     # サービス層ロジックの in-memory SQLite ユニットテスト
```

UI テストは `AI_PROVIDER=stub`・専用一時 SQLite DB で起動したサーバーに対して Chromium を操作する。DB はテストセッション内で共有されるため、各テストは一意な単語名を使い干渉を避ける。

ユニットテストは独立した in-memory SQLite（`create_engine("sqlite:///:memory:")`）を使い、サーバー起動やブラウザを介さずサービス層の関数を直接呼び出して検証する。出題優先度のような、ブラウザ越しに決定的な検証が難しいロジックに用いる。

### 静的解析（lint）

ruff を `[tool.ruff]` / `[tool.ruff.lint]`（`server_app/pyproject.toml`）で設定し、`uv run ruff check .` で実行する。
`.github/workflows/server_app_lint.yml` が `server_app/**` の変更時に自動実行し、main ブランチへのマージにはこのチェックの成功が必須（ブランチ保護で required status check に設定）。

SQLAlchemy のクエリ条件式（`Column == True/False`）は ruff の E712 で警告されるため、`.is_(True)` / `.is_(False)` を使う。
`Mapped["Word"]` のような文字列フォワード参照（循環インポート回避）は ruff が未定義名として誤検知するため `# noqa: F821` を付与する。

### 各 feature モジュールの構成

```
features/<name>/
├── models.py    # SQLAlchemy モデル
├── schemas.py   # Pydantic スキーマ
├── router.py    # FastAPI エンドポイント
└── service.py   # ビジネスロジック
```

---

## 技術スタック

| 区分 | 技術 | バージョン |
|------|------|-----------|
| Web フレームワーク | FastAPI | >=0.115.0 |
| ORM | SQLAlchemy | >=2.0.0 |
| DB | SQLite | - |
| テンプレート | Jinja2 | >=3.1.0 |
| 設定管理 | pydantic-settings | >=2.6.0 |
| AI (Claude) | anthropic | >=0.40.0 |
| AI (Gemini) | google-genai | >=1.0.0 |
| UI テスト | playwright / pytest-playwright | >=0.5.0 |
| 静的解析 | ruff | >=0.8.0 |
| スペルチェック | pyspellchecker | >=0.9.0 |
| フォーム解析 | python-multipart | >=0.0.12 |
| ランタイム | Python | >=3.12 |
| パッケージ管理 | uv | - |

---

## データベース設計

### ER 図（テーブル関係）

```
Word (1) ─── (N) ExampleSentence
Word (1) ─── (N) WordSource
Word (1) ─── (N) QuizAnswer
Word (1) ─── (N) QuizSessionWord

QuizSession (1) ─── (N) QuizSessionWord
QuizSession (1) ─── (N) QuizAnswer
```

### テーブル一覧

| テーブル名 | 概要 |
|-----------|------|
| `words` | 単語マスタ |
| `example_sentences` | 例文（単語に紐づく） |
| `word_sources` | 出典情報（単語に紐づく） |
| `quiz_sessions` | クイズセッション |
| `quiz_session_words` | セッション内の出題単語 |
| `quiz_answers` | 回答記録 |
| `study_records` | 学習日記録（ストリーク用） |

### words テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| word | VARCHAR(100) | UNIQUE, INDEX, NOT NULL |
| reading | VARCHAR(200) | NULL |
| meaning | TEXT | NOT NULL |
| part_of_speech | VARCHAR(50) | NULL |
| created_at | DATETIME | NOT NULL (UTC) |
| updated_at | DATETIME | NOT NULL (UTC) |

### example_sentences テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| word_id | INTEGER | FK(words.id), CASCADE DELETE |
| sentence_en | TEXT | NOT NULL |
| sentence_ja | TEXT | NOT NULL |
| order | INTEGER | NOT NULL |
| label | VARCHAR(50) | NULL（品詞ラベル） |

### word_sources テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| word_id | INTEGER | FK(words.id), CASCADE DELETE |
| source_type | VARCHAR(20) | NOT NULL（book/website/video/other） |
| title | VARCHAR(300) | NULL |
| url | VARCHAR(500) | NULL |
| page_number | VARCHAR(50) | NULL |
| detail | TEXT | NULL |
| created_at | DATETIME | NOT NULL (UTC) |

### quiz_sessions テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| started_at | DATETIME | NOT NULL |
| completed_at | DATETIME | NULL |

### quiz_session_words テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| session_id | INTEGER | FK(quiz_sessions.id), CASCADE DELETE |
| word_id | INTEGER | FK(words.id), CASCADE DELETE |
| order | INTEGER | NOT NULL |
| is_answered | BOOLEAN | NOT NULL |

### quiz_answers テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| session_id | INTEGER | FK(quiz_sessions.id), CASCADE DELETE |
| word_id | INTEGER | FK(words.id), CASCADE DELETE |
| is_correct | BOOLEAN | NOT NULL |
| answered_at | DATETIME | NOT NULL |

### study_records テーブル

| カラム | 型 | 制約 |
|--------|----|------|
| id | INTEGER | PK, AUTO |
| study_date | DATE | UNIQUE, INDEX, NOT NULL |
| words_studied | INTEGER | NOT NULL |
| quiz_completed | INTEGER | NOT NULL |

---

## 設定・環境変数

`server_app/.env` に記述。Pydantic BaseSettings で管理。

| 変数名 | デフォルト | 説明 |
|--------|-----------|------|
| `APP_NAME` | `Language Learn App` | アプリ名 |
| `DEBUG` | `False` | デバッグモード（SQLAlchemy echo + uvicorn reload） |
| `DATABASE_URL` | `sqlite:///data/language_learn.db` | DB 接続文字列 |
| `AI_PROVIDER` | `claude` | 使用 AI プロバイダ（`claude` / `gemini` / `stub`。`stub` はテスト専用） |
| `ANTHROPIC_API_KEY` | `""` | Claude API キー |
| `GOOGLE_API_KEY` | `""` | Gemini API キー |
| `GEMINI_MODEL` | `gemini-2.0-flash` | Gemini モデル名 |
| `AI_STUB_FORCE_RATE_LIMIT` | `False` | `stub` プロバイダ使用時、true ならレート制限エラーを強制発生（テスト専用） |
| `QUIZ_QUESTION_COUNT` | `20` | 1セッションの出題数 |

---

## AI プロバイダ設計

プロバイダは `BaseAIProvider` 抽象クラスで統一インターフェースを実装。

```python
class BaseAIProvider:
    def generate_word_info(self, word: str) -> AIGenerateResponse: ...
```

| プロバイダ | クラス | モデル | 出力方式 |
|-----------|--------|--------|---------|
| Claude | `ClaudeProvider` | claude-haiku-4-5-20251001 | JSON テキスト解析 |
| Gemini | `GeminiProvider` | gemini-2.0-flash | response_schema 構造化出力 |
| Stub（テスト専用） | `StubProvider` | なし（実 API 呼び出しなし） | 固定の `AIGenerateResponse` を即時返却。`AI_STUB_FORCE_RATE_LIMIT=1` で `AIRateLimitError` を強制発生 |

### エラーハンドリング

| 例外クラス | 用途 |
|-----------|------|
| `AIServiceError` | API 呼び出し失敗・レスポンス解析失敗時の基底例外。HTTP 500 を返す |
| `AIRateLimitError` | `AIServiceError` のサブクラス。クォータ超過・レート制限（429）時に送出し、`retry_after_seconds` に再試行までの待機秒数を保持する。HTTP 429 を返す |

各プロバイダは API クライアントが返すレート制限エラー（Gemini: `google.genai.errors.APIError` で `code == 429`、Claude: `anthropic.RateLimitError`）を捕捉し、レスポンスから再試行待機秒数を抽出して `AIRateLimitError` に変換する。

> **注意（htmx のスワップ挙動）**: htmx はデフォルトで 2xx 以外のレスポンスをスワップしない。
> `/api/ai/generate` が 429/500 を返す際にエラーパーシャルを表示するため、
> `words/add.html` で `htmx:beforeSwap` をハンドリングし、`#ai-fields` への
> レスポンスに限り `shouldSwap = true` / `isError = false` を強制している
> （UI テスト追加時に発見・修正）。

### AI レスポンス構造

```json
{
  "reading": "IPA表記の読み",
  "word_meanings": [
    {
      "brand": "品詞",
      "meaning": "日本語の意味",
      "examples": [
        {"sentence_en": "英語例文1", "sentence_ja": "日本語訳1"},
        {"sentence_en": "英語例文2", "sentence_ja": "日本語訳2"}
      ]
    }
  ]
}
```

---

## クイズ優先度選択アルゴリズム（`select_quiz_words`）

1. **Tier 1**: 未テスト単語（`quiz_answers` に記録なし）→ ランダム順
2. **Tier 2（未出題期間優先スロット）**: Tier 1 で埋まらなかった残りスロットのうち、
   `STALE_WORD_QUOTA_RATIO`（0.1 = 10%、最低1件）分は、正答率に関わらず
   `MAX(quiz_answers.answered_at)` が最も古い（最後に出題してから最も時間が経過している）
   テスト済み単語を優先的に採用する。正答率が高い単語でも定期的に再出題されるようにするための仕組み。
3. **Tier 3**: 残りのテスト済み単語のうち正解率が低い順（`correct / total` 昇順）
4. **Tier 4**: それ以外の残り単語 → ランダム順

設定数（`QUIZ_QUESTION_COUNT`、デフォルト20）に達するまで各 Tier から取得し、最後に出題順をシャッフルする。

ユニットテスト: `server_app/tests/unit/test_quiz_priority.py`（in-memory SQLite で `select_quiz_words` を直接検証）。

---

## マイグレーション

`main.py` 起動時に自動実行:
- `example_sentences.label` カラムが存在しない場合に `ALTER TABLE` で追加

---

## 認証・認可

**現在未実装**。シングルユーザー用アプリとして設計されており、認証機構なし。

---

## 起動方法

```bash
cd server_app
uv run start   # uvicorn で 0.0.0.0:8000 起動
```
